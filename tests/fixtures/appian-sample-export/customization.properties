# Synthetic Appian application export fixture — NOT a real customer export.
# Used only to test agent-driven extraction (workflow-logic-extractor,
# workflow-dependency-mapper, workflow-forms-generator) against something
# with Appian's characteristic export shape, without any real customer data.
#
# Real Appian exports map opaque UUIDs to human names here; this fixture
# uses human-readable names directly for readability, since the extraction
# agents work from content, not from this mapping file.
application.name=Expense Approval Demo
application.prefix=EXP
export.appian.version=23.4
